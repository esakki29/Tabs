# App for pagination with tabs

## Available Scripts

In the project directory, you can run:

## `npm install`

Install dependencies

### `npm start`

Runs the app in the development mode.\
Open [http://localhost:3000]
